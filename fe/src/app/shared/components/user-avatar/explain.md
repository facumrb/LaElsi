# 📂 `user-avatar/` — Avatar de Usuario

Componente que muestra la **foto de perfil** del usuario. Si el usuario tiene foto cargada, la muestra usando la URL del backend (`environment.userImagesUrl`). Si no tiene foto, muestra las **iniciales** del nombre como fallback. Se usa en navbars, perfiles y tablas del admin.
