# 📂 `pagination/` — Componente de Paginación

Componente que renderiza los **controles de paginación** (botones de página, anterior/siguiente). Se usa en todas las tablas del admin y en los listados de productos del e-commerce. Recibe el total de páginas y la página actual, y emite un evento al cambiar de página.
