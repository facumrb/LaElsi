# 📂 `product-image/` — Imagen de Producto

Componente que muestra la **foto de un producto** con manejo de fallback. Si el producto tiene fotos, muestra la primera; si no tiene, muestra una imagen placeholder por defecto. Construye la URL completa usando `environment.productImagesUrl`.
