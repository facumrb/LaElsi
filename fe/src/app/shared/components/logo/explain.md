# 📂 `logo/` — Logo de LaElsi

Componente que renderiza el **logo de LaElsi**. Se reutiliza en la barra de navegación, footer, páginas de auth, etc. Puede aceptar inputs para controlar el tamaño o variante del logo (claro/oscuro).
