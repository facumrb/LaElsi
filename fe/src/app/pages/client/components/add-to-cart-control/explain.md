# 📂 `add-to-cart-control/` — Control de Agregar al Carrito

Componente con un **selector de cantidad** (botones +/-) y el botón **"Agregar al carrito"**. Se usa dentro de las tarjetas de producto y en la página de detalle de producto. Interactúa con `CartService` para agregar ítems.
