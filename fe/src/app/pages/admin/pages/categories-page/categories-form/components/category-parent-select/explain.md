# 📂 `category-parent-select/` — Selector de Categoría Padre

Componente de **select/dropdown** que permite elegir una categoría padre al crear o editar una categoría. Carga las categorías disponibles desde la API y las presenta en un dropdown, permitiendo crear una estructura jerárquica de categorías.
