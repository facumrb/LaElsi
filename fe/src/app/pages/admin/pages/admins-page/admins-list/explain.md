# 📂 `admins-list/` — Lista de Administradores

Componente de **tabla** que muestra los administradores registrados. Columnas típicas: nombre completo, email, username, estado (activo/eliminado), y acciones (editar, eliminar, restaurar). Recibe los datos desde el componente padre.
