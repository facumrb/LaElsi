# 📂 `table-actions/` — Acciones de Tabla

Componente que renderiza los **botones de acción** en cada fila de las tablas del admin. Típicamente incluye: botón Editar (navega al formulario), botón Eliminar (con confirmación), y botón Restaurar (para entidades eliminadas lógicamente). Emite eventos al componente padre.
