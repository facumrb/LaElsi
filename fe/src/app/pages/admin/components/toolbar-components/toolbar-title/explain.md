# 📂 `toolbar-title/` — Título de Página del Admin

Componente simple que renderiza el **título** de la sección actual del panel admin (ej: "Productos", "Categorías", "Pedidos"). Recibe el texto como input y lo muestra con el estilo de encabezado consistente del panel.
