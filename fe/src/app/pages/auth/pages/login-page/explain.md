# 📂 `login-page/` — Página de Inicio de Sesión

Página con el **formulario de login** (ruta `/auth/login`). Campos de usuario/email y contraseña con validaciones. Al autenticar exitosamente, redirige según el rol del usuario: admin → `/admin`, cliente → `/`. Incluye link al formulario de registro.
